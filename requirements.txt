# Runtime dependencies for routines/ (Condor) and controllers/ (Hummingbot V2).
# hummingbot itself, python-telegram-bot, and condor's own package are provided by
# their respective runtimes (a real Hummingbot install, a real Condor install) — not
# listed here, since this repo's controllers/routines are meant to be dropped into
# those, not to vendor them. See docs/dry-run-runbook.md.
httpx>=0.27
pyyaml>=6.0
pydantic>=2.0
